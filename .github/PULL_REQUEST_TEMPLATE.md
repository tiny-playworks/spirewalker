## 变更说明

- 简要描述这次改动做了什么、为什么要做。

## 自检清单

- [ ] 已在本地执行 `pnpm lint`
- [ ] 已在本地执行 `pnpm test`
- [ ] 只包含本次目标相关改动
- [ ] 如有行为变化，已补充或更新测试

## 分支约定

- `feature/*` -> `dev`
- `fix/*` -> `dev`
- `chore/*` -> `dev`
- 仅 `dev` -> `main` 用于发布/稳定合并
